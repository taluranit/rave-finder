{
    "actorKeyValueStoreSchemaVersion": 1,
    "title": "Run state",
    "description": "Internal state the Actor carries between runs: cached geocoding results.",
    "collections": {
        "geocodeCache": {
            "title": "Geocode cache",
            "description": "Place name/address to coordinates, cached so repeat runs don't re-query Nominatim for places already resolved. Only successful lookups are cached — a failed one is retried next run rather than replaying the failure forever.",
            "key": "GEOCODE_CACHE",
            "contentTypes": [
                "application/json"
            ]
        }
    }
}
